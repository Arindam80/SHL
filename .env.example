# SHL Assessment Recommendation System - Environment Configuration

# =============================================================================
# FREE MODE CONFIGURATION (No API Keys Required)
# =============================================================================
# This system works 100% FREE without any paid API services!
# You don't need to configure anything below unless you want to enable
# optional LLM re-ranking (which requires a paid OpenAI account).

# OpenAI API Configuration (OPTIONAL - for LLM re-ranking only)
# Leave empty to use FREE mode with semantic search only
OPENAI_API_KEY=

# Use LLM Re-ranking (true/false)
# Set to false for 100% FREE mode
USE_LLM_RERANKING=false

# Embedding Model (LOCAL - FREE)
# This model runs on your machine without any API calls
EMBEDDING_MODEL=all-MiniLM-L6-v2

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# =============================================================================
# HOW TO USE:
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Keep USE_LLM_RERANKING=false for FREE mode
# 3. Start the server: python src/api.py
# 4. Open http://localhost:8000
#
# That's it! No API keys needed! 🎉
