# Vercel Configuration for Frontend

This directory contains the frontend for the SHL Assessment Recommendation System.

## Environment Variables

After deploying the backend to Render, update the API URL in `index.html`:

Replace `YOUR-APP-NAME.onrender.com` with your actual Render URL.
